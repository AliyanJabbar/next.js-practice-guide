// if we want to add a route or a page in our website than we have to add a folder in our (src or app or main route) than make an end point(page.tsx) in that folder

export default function About (){
    return (
        <div>
            <h3>About Page</h3>
        </div>
    )
}