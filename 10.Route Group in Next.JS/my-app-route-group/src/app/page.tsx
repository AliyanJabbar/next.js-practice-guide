//Route Group is a gift given by next.js
//by using route group we can organize our structure
// A route group can be created by wrapping a folder's name in parenthesis: (folderName)
// route group ko hum saaf suthra kaam krne ke liye use krte hain. we can also make another layout.tsx file inside a route so the layout will be apply only on the route group

export default function Home() {
  return (
    <div><h1>Route Group</h1></div>
  );
}